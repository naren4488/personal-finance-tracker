export {
  handleAuthApiError,
  handleAuthApiErrorIfNeeded,
  isAuthFailureMessage,
  isAuthTokenRequiredMessage,
} from "@/lib/auth/handle-auth-api-error"

/** Alias kept for existing form imports. */
export { handleAuthApiError as handleFormApiError } from "@/lib/auth/handle-auth-api-error"
